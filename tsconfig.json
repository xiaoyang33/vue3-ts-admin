{
  "compilerOptions": {
    // 目标代码(ts -> js(es5/6/7))选择的babel转化不需要配置
    "target": "esnext",
    // 目标代码需要使用的模块化方案(commonjs require/module.exports esmodule import/export)
    "module": "esnext",
    // 严格检查(any)
    "strict": true,
    // 对jsx进行怎样的处理
    "jsx": "preserve",
    // 辅助的导入功能
    "importHelpers": true,
    // 按照node的方式去解析模块 import
    "moduleResolution": "node",
    // 跳过对一些库的类型检测 (axios -> 类型 )
    "skipLibCheck": true,
    // export default/module.exports 能不能混合使用
    // 两个设置为true 可以混合使用
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    // 要不要生成映射文件 ts->js
    "sourceMap": true,
    // 文件路径解析时 进本url
    "baseUrl": ".",
    // 指定具体要解析使用的类型
    "types": ["webpack-env"],
    // 别名路径解析
    "paths": {
      "@/*": ["src/*"],
      "components/*":["src/components/*"]
    },
    // 指定可以在项目中使用哪些库的类型(Proxy/Window/Document)
    "lib": ["esnext", "dom", "dom.iterable", "scripthost"]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "tests/**/*.ts",
    "tests/**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
